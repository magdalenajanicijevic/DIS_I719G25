apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ecommerce

spec:
  replicas: 1

  selector:
    matchLabels:
      app: order-service

  template:
    metadata:
      labels:
        app: order-service

    spec:
      containers:
        - name: order-service
          image: magdalenajanicijevic/order-service:latest

          ports:
            - containerPort: 8084

          env:
            - name: EUREKA_HOST
              value: discovery-server

            - name: DB_HOST
              value: postgres

            - name: DB_USERNAME
              value: postgres

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: DB_PASSWORD

            - name: RABBITMQ_HOST
              value: rabbitmq

            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: RABBITMQ_USERNAME

            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: RABBITMQ_PASSWORD

            - name: SPRING_CONFIG_IMPORT
              value: optional:configserver:http://config-server:8888

---

apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce

spec:
  selector:
    app: order-service

  ports:
    - port: 8084
      targetPort: 8084

  type: ClusterIP