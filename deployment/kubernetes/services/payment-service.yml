apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: ecommerce

spec:
  replicas: 1

  selector:
    matchLabels:
      app: payment-service

  template:
    metadata:
      labels:
        app: payment-service

    spec:
      containers:
        - name: payment-service
          image: magdalenajanicijevic/payment-service:latest

          ports:
            - containerPort: 8085

          env:
            - name: EUREKA_HOST
              value: discovery-server

            - name: DB_HOST
              value: postgres

            - name: DB_USERNAME
              value: postgres

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: DB_PASSWORD

            - name: RABBITMQ_HOST
              value: rabbitmq

            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: RABBITMQ_USERNAME

            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: RABBITMQ_PASSWORD

            - name: SPRING_CONFIG_IMPORT
              value: optional:configserver:http://config-server:8888

---

apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: ecommerce

spec:
  selector:
    app: payment-service

  ports:
    - port: 8085
      targetPort: 8085

  type: ClusterIP